export const DEFAULT_DAILY_TOKEN_QUOTA = 10_000;
export const DEFAULT_IP_RATE_PER_MINUTE = 100;

export const API_PATHS = Object.freeze({
  health: '/api/health',
  chat: '/api/chat',
  voice: '/api/voice',
  usage: '/api/me/usage',
});
